build:
  docker:
    worker: Dockerfile
    web: Dockerfile
run:
  web: python3 lichess-bot.py -u
  worker:
    command:
      - chmod +x %s" % (engine, engine) && chmod +x ./engine/stockfishssse && python3 lichess-bot.py -u
    image: worker
